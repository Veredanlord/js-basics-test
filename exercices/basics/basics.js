/* JS basics */
/* Try to use ES6 since it simplifies a lot of code */

/* Exo 1 */
/* The output must be a list with no negative numbers */

export function exo1(numbers) { // input example : [1, 5, 6, -5, -2, 0, 19]
    return [];
}

/* ================================================== */
/* Exo 2 */
/* The output must be a list with no duplicates */
/* hint: There are 3 different ways you can do it */

export function exo2(numbers) { // input example : [1, 12, 1, 5, 0, 55, 12]
    return [];
}

/* ================================================== */
/* Exo 3 */
/* The output must be the input list with all numbers returned as positives */
/* example: [0, 5, -4, -18] => [0, 5, 4, 18]*/

export function exo3(numbers) { // input example : [0, 5, -4, -18]
    return [];
}